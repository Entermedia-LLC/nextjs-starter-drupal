langcode: en
status: true
dependencies:
  config:
    - user.role.next
  module:
    - user
id: user_remove_role_action.next
label: 'Remove the Next role from the selected user(s)'
type: user
plugin: user_remove_role_action
configuration:
  rid: next
